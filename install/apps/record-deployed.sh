#!/bin/bash

# Запоминает, что именно было развёрнуто на машину.
#
# Без этой записи обновление не может отличить три разных случая: репозиторий
# ушёл вперёд, пользователь правил живой файл, или изменились обе стороны. Оно
# видело бы просто "файлы разные" и предлагало заменить все подряд -- в том
# числе чужие настройки.
#
# Идёт последним среди apps: к этому моменту развёрнуто всё -- и config/, и
# .desktop с иконками, и программы из install/apps/files.

omvoid-refresh-config --record
